<!--
  author: wei
  date: 2026-04-20
-->

# 裸 WebSocket 与原生 WebRTC：连续实时音频场景对比

## 0. 场景与约束（已定稿）

| 项 | 取值 / 要求 |
|----|----------------|
| 业务形态 | **全双工**（双向同时有语音） |
| 对比对象 | **裸 WebSocket**（自建协议栈） vs **原生（Native）WebRTC** |
| 媒体形态 | 连续实时音频；约 **50 ms 一帧、每帧约 400 B**（量级约 64 kbps，不含协议头） |
| 端到端延迟 | **≤ 400 ms**（硬约束） |
| 弱网 | **高丢包**、**高抖动**、**间歇性断线重连** 均需考虑 |
| 成功 KPI | **可懂度**（主观/客观可懂，如 STOI/PESQ/MOS 等工程化目标） |

---

## 1. 总览结论（先看结论）

在 **全双工 + 弱网 + E2E ≤ 400 ms + 可懂度优先** 下，**原生 WebRTC 通常更占优**：其媒体面为 **UDP + RTP/SRTP**，配套 **抖动缓冲、丢包恢复、带宽/延迟估计、声学回声消除（AEC）** 等，与「连续小块音频」天然匹配。

**裸 WebSocket** 多在 **TCP** 上，弱网下易出现 **队头阻塞导致延迟膨胀**，对 **400 ms 预算** 与 **可懂度** 风险更高；若坚持用 WebSocket，需要在应用层补齐 **类 RTP 的时序语义、自适应缓冲、丢包隐藏、重连与媒体状态机**，工程量接近「自研半套 WebRTC」。

---

## 2. 全双工维度

| 维度 | 裸 WebSocket | 原生 WebRTC |
|------|----------------|-------------|
| 双向通道 | 需自建 **上行/下行** 语义（单连接双工或双连接）、**序列号/时间戳**、**同步策略** | **PeerConnection** 天然双工；音频轨上下行分离清晰 |
| 回声 | 必须集成 **AEC**（软件或硬件），并与 **采集/播放线程、缓冲** 对齐 | **WebRTC APM** 管线成熟，与 **jitter buffer** 协同 |
| 全双工下的弱网 | TCP 一侧阻塞可 **同时拖垮听与说** 的时延预算 | UDP 媒体面可 **独立处理** 上下行丢包与缓冲（仍受 TURN/ICE 影响） |

**结论**：全双工下 **可懂度** 强依赖 **AEC + 稳定时钟 + 合理丢包策略**；原生栈集成度更高。

---

## 3. 端到端延迟（≤ 400 ms）

| 因素 | 裸 WebSocket | 原生 WebRTC |
|------|----------------|-------------|
| 传输层 | **TCP**：重传、拥塞控制、**队头阻塞** → 弱网下 **尾部延迟**不可控 | **UDP**：单包丢失未必阻塞后续；配合 **GCC/NCC** 等控制发送节奏 |
| 缓冲策略 | 自研 **jitter buffer**：过小 → 卡顿；过大 → **超 400 ms** | 内置 **自适应 jitter buffer**；仍需调参以满足 **400 ms** |
| 编解码与 PLC | 若用带 **DTX/CNG** 的编码器，需与 **帧长 50 ms** 对齐；丢包时 **PLC** 自建或外接 | **Opus** 等与 WebRTC 集成成熟，**PLC/FEC** 路径清晰 |
| ICE/TURN | 无（若纯 WS），穿透简单但 **无标准 P2P 建连**；跨 NAT 常靠服务端中继 | **ICE + STUN/TURN**：TURN 多一跳延迟，但 **可用性** 往往更好 |

**结论**：**400 ms** 在弱网下是紧预算；WebSocket 方案需证明 **P99 端到端** 仍达标；WebRTC 更容易通过 **行业默认调参 + 客观指标** 逼近目标。

---

## 4. 弱网：高丢包、高抖动、间歇断线重连

### 4.1 高丢包

| 项 | 裸 WebSocket | 原生 WebRTC |
|----|----------------|-------------|
| 丢包表现 | TCP **重传**，吞吐下降；实时音频更怕 **迟到** 而非「永远不到」 | **NACK / FEC（可选）**、**PLC**；可主动 **丢弃过期音频块** 保时间轴 |
| 可懂度 | 若不及时丢弃迟到块，**重叠/乱序播放** 伤害可懂度 | **Jitter buffer + NetEq 类** 机制更成熟 |

### 4.2 高抖动

| 项 | 裸 WebSocket | 原生 WebRTC |
|----|----------------|-------------|
| 抖动吸收 | 自研缓冲与 **播放时钟**；与 TCP 到达模式耦合 | **Jitter buffer** 与 **timestamp** 驱动播放 |
| 与 50 ms 节拍 | 需 **锁相** 或 **重采样** 对齐设备时钟 | 同样要调参，但工具链多 |

### 4.3 间歇性断线重连

| 项 | 裸 WebSocket | 原生 WebRTC |
|----|----------------|-------------|
| 连接语义 | **WS 重连** 简单；**媒体状态**（序列、时间基准、DTLS/SRTP 若自建）复杂 | **ICE restart**、**DTLS resume**、**m-line 重建** 等有现成实践 |
| 可懂度 | 重连瞬间若无 **淡入淡出/舒适噪声** 易出现爆音/断续 | 同样需产品层处理，但 **底层恢复路径** 更标准化 |

---

## 5. 成功 KPI：可懂度

| 项 | 裸 WebSocket | 原生 WebRTC |
|----|----------------|-------------|
| 客观/半客观指标 | 可接 **PESQ/STOI** 等，但需保证 **测试链路与真实弱网一致** | 同样可测；**NetEQ/Opus** 组合业界样本多，**对标与调参**成本低 |
| 主观 MOS | 强依赖 **AEC、PLC、抖动、延迟** 综合；WS 方案 **集成风险高** | 更容易达到「可用电话音质」基线 |
| 与延迟矛盾 | 为可懂度加缓冲 → 易顶 **400 ms** 红线 | 需在 **buffer 目标 vs 可懂度** 间做 **GCC 参数与 max delay** 联合调优 |

---

## 6. 选型建议（在本文约束下）

1. **优先推荐：原生 WebRTC（Native）** 作为默认媒体承载，满足 **全双工、弱网、可懂度、≤400 ms（需压测验证 P95/P99）** 的综合概率更高。
2. **裸 WebSocket** 更适合：强管控网络、延迟不敏感、或 **仅信令**；若坚持 WS 传音频，应评估 **是否最终仍会复制 RTP/WebRTC 子集**。
3. **工程落地**：无论选哪条，都建议建立 **弱网仿真（丢包/抖动/断连）+ 端到端延迟分桶 + 可懂度客观指标** 的 **门禁**，再锁版本与参数。

---

## 7. 与「50 ms / 400 B」节奏的额外说明

- **50 ms** 与常见 **Opus 20 ms 帧** 不同；若固定 50 ms 一包，需在 **打包/解包、PLC、FEC 块对齐** 上统一设计。
- **400 B** 需明确是否 **含 WS/RTP/SRTP 头**；对比 WebRTC 时应用 **同等口径** 算有效音频码率。
- **全双工** 下建议 **独立统计** 上下行 **丢包、抖动、播放欠载/过载** 与 **端到端延迟**，避免只看单向。

---

*本文档为架构层对比，不构成对具体厂商 SDK 的承诺；上线前请以压测与可懂度指标为准。*
## 补充说明

根据您提供的交互对话场景（延迟 < 400ms）以及移动/3G 网络环境，**WebRTC** 在弱网下具备更优的抗丢包与低延迟能力，这是 WebSocket 无法比拟的。

---

### 核心结论

在您的业务场景下，WebRTC 能显著提升通话的成功率和流畅度；而 WebSocket 由于 TCP 的固有缺陷，极易出现延迟超标及卡顿加剧的问题。

---

### 详细对比

| 对比维度     | WebRTC         | WebSocket         | 对场景的影响 |
|--------------|----------------|------------------|--------------|
| 底层协议     | UDP + 拥塞控制 | TCP（可靠、有序）| 决定性差异，TCP 队头阻塞在弱网下尤为致命 |
| 弱网对抗机制 | 原生支持 FEC、NACK 重传、NetEQ 抗抖动、自适应码率 | 几乎无，仅依赖 TCP 重传，延迟难以控制 | WebRTC 能在 60% 丢包下保障流畅，WebSocket 则常卡顿 |
| 端到端延迟   | 可控在 200ms 内，优化后更低 | 高且不稳定，3G 下平均可达 800ms，易超 400ms | WebRTC 满足实时交互要求，WebSocket 很难达到 |
| 3G适应性     | 优秀，支持低码率编解码和带宽自适应 | 差，仅固定码率，无法动态调整 | WebRTC 可动态降至 6-64Kbps 保通，WebSocket(400B/50ms ≈64Kbps)易因拥塞失败 |
| 开发复杂度   | 高（需实现信令、STUN/TURN、媒体协商等） | 低（建立连接即可收发） | WebRTC 的复杂度换取了可靠性 |
| 连接建立     | 稍慢（ICE 协商） | 快速（HTTP Upgrade） | 长会话通话，建立时延可忽略 |

---

### 关键机制分析

1. **队头阻塞（Head-of-Line Blocking）是 WebSocket 的致命问题**
   - 50ms 节拍下，只要一个 TCP 包丢失或延迟，后续所有音频都会阻塞，等待重传。在 3G 高抖动网络下，延迟会不断累加，极易突破 400ms 阈值。会造成对话频繁卡断、“抢话”等问题。

2. **WebRTC 的组合抗弱网方案**
   - **抗丢包**：支持 FEC（前向纠错），接收端能 NACK 选择性重传关键包，优先保证最新数据到达。
   - **抗抖动**：NetEQ 动态调整抖动缓冲，平滑音频播放，在消除抖动同时保持更低延迟。
   - **自适应**：内置带宽评估和编码码率自适应（GCC 算法），移动网络波动时也可保障通话。

3. **数据印证**
   - 专业 RTC 服务在 3G 网络下仍能保障全球端到端延迟 <400ms。
   - WebSocket 在高延迟和高抖动网络下，乱序与丢包率激增，语音可懂度严重下滑。

---

### 最终建议

- **强烈推荐使用 WebRTC**，其是实时音视频通信的业界标准，能够解决您当前的核心问题。尽管开发门槛较高，但能显著提升用户体验，保障可用性。
- 如果坚持使用 WebSocket，需充分认识到其不适用于移动网络下的实时语音。只有在绝大部分用户身处高速 Wi-Fi 且能容忍卡顿时才适用，即便如此还需自行实现复杂的冗余、纠错、乱序处理（实际上是在“重造轮子”）。

---

**结论**：为了保证移动/3G 场景下，400ms 内的实时语音对话成功率和流畅度，应当坚定选择 WebRTC。
